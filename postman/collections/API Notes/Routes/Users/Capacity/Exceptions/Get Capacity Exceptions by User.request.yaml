$kind: http-request
description: >-
  Gets capacity exception records for a specific user.


  `capacityMinutes` is the user's available capacity **per day** for each date
  in the exception range.


  Query behavior:

  - `locations` scopes by user location membership.

  - `startDate`/`endDate` filter by date overlap (YYYY-MM-DD).

  - `cursor`/`limit` paginate results.


  Response fields include:

  `exceptionID`, `userID`, `createdByUserID`, `exceptionStartDate`,
  `exceptionEndDate`, `capacityMinutes`, `hoursPerDay`, `minutesPerDay`,
  `exceptionTitle`, `exceptionNotes`.
url: "{{protocol}}://{{server}}:{{port}}/v2/users/capacity/exceptions/:userID?l\
  ocations=1&startDate=2026-01-01&endDate=2026-01-31&cursor=0&limit=100"
method: GET
queryParams:
  - key: locations
    value: "1"
    description: Required comma-separated location IDs used to scope the user access
      context.
  - key: startDate
    value: 2026-01-01
    description: Optional lower date bound (YYYY-MM-DD) for overlap filtering.
  - key: endDate
    value: 2026-01-31
    description: Optional upper date bound (YYYY-MM-DD) for overlap filtering. Must
      be >= startDate when both are present.
  - key: cursor
    value: "0"
    description: Optional ID cursor for keyset pagination.
  - key: limit
    value: "100"
    description: Optional max rows per page.
pathVariables:
  userID: ""
examples: ./.resources/Get Capacity Exceptions by User.resources/examples
order: 1000
