$kind: http-request
description: This request gets all of the Roles a User has and what Location
  they have that Role at.
url: "{{protocol}}://{{server}}:{{port}}/v2/users/:userID/roles"
method: GET
queryParams:
  - key: roles
    value: 1,2
    disabled: true
    description: This parameter expects a comma-separated list of roleIDs to filter teams by
  - key: locations
    value: 163,202
    disabled: true
    description: This parameter expects a comma-separated list of locationIDs to
      filter roles by
  - key: name
    value: "%Manager%"
    disabled: true
    description: This parameter is used to only get specific role by name. This
      parameter expects a string full name of a user or partial name with the
      wildcard %.
  - key: cursor
    value: "0"
    disabled: true
    description: This parameter is a cursor that selects what teamID you want to
      start receiving results at. e.g. passing 137 here will only get you tasks
      with an id greater than 137.
  - key: limit
    value: "100"
    disabled: true
    description: This parameter is a result limiter. The default is set to return no
      more than 100 results at one time.
pathVariables:
  - key: userID
    value: "{{userID}}"
    description: userID of the user that you wish to query Roles for.
scripts:
  - type: afterResponse
    code: "pm.test(\"response must be valid and have a body\", function () {\r

      \     // assert that the status code is 200\r

      \     pm.response.to.be.ok; // info, success, redirection,
      clientError,  serverError, are other variants\r

      \     // assert that the response has a valid JSON body\r

      \     pm.response.to.be.withBody;\r

      \     pm.response.to.be.json; // this assertion also checks if a
      body  exists, so the above check is not needed\r

      });"
    language: text/javascript
examples: ./.resources/Get User Roles.resources/examples
order: 1000
